'use client';
import {useEffect,useState} from 'react';
const defaults=['Education','Authority','Sales','Personal','Behind the Scenes','Psychology','Faith & Values','Inspiration','Entertainment'];
const ideas=[
{pillar:'Authority',format:'Reel',hook:'3 mistakes keeping your audience from trusting your content.'},
{pillar:'Education',format:'Carousel',hook:'Stop asking what to post. Ask these 3 questions instead.'},
{pillar:'Psychology',format:'Reel',hook:'Why people scroll past useful content — and how to stop them.'},
{pillar:'Faith & Values',format:'Post',hook:'The values behind the way I choose to show up online.'},
{pillar:'Behind the Scenes',format:'Story',hook:'Here is what planning a week of content actually looks like.'}];
function makePost(i,profile){const x=ideas[i%ideas.length];return{...x,goal:profile.goal||'Build authority',script:`If you are a ${profile.business||'business owner'} trying to reach ${profile.audience||'the right audience'}, keep this simple. Start with one clear problem, teach one useful solution, and give people one next step. Your content does not need to impress everyone — it needs to help the right person.`,shot:'Start face-to-camera with the hook. Cut to three short on-screen points. Finish face-to-camera with the CTA.',caption:`Good content starts with clarity. Know who you help, solve one real problem and make the next step obvious. That is how ${profile.business||'your business'} turns content into trust.`,cta:'Save this and use it before creating your next post.'}}
export default function Home(){const [screen,setScreen]=useState('home'),[idx,setIdx]=useState(0),[saved,setSaved]=useState([]),[toast,setToast]=useState(''),[ready,setReady]=useState(false),[profile,setProfile]=useState({business:'',audience:'',goal:'Grow my audience',platform:'Instagram',pillars:['Education','Authority','Psychology']});useEffect(()=>{try{const p=localStorage.getItem('cc-profile'),s=localStorage.getItem('cc-saved');if(p){setProfile(JSON.parse(p));setReady(true)}if(s)setSaved(JSON.parse(s))}catch{}},[]);const persist=(p)=>{setProfile(p);localStorage.setItem('cc-profile',JSON.stringify(p));setReady(true);setScreen('home')};const post=makePost(idx,profile);const save=()=>{const next=[post,...saved.filter(x=>x.hook!==post.hook)];setSaved(next);localStorage.setItem('cc-saved',JSON.stringify(next));setToast('Saved to Idea Bank ✓');setTimeout(()=>setToast(''),1500)};const copy=async()=>{await navigator.clipboard?.writeText(post.caption);setToast('Caption copied ✓');setTimeout(()=>setToast(''),1500)};return <main className="shell"><header><div className="brand"><span className="logo">✦</span><div><b>CONTENT COMPASS</b><small>{profile.business||'Never wonder what to post again.'}</small></div></div><button className="avatar" onClick={()=>setScreen('setup')}>RS</button></header>{!ready&&screen==='home'?<Setup profile={profile} save={persist}/>:<>{screen==='home'&&<><section className="hero"><span className="eyebrow">YOUR CONTENT CO-PILOT</span><h1>What will you<br/><em>create today?</em></h1><p>Ideas shaped around {profile.audience||'your audience'} and your goal to {profile.goal.toLowerCase()}.</p></section><button className="action primary" onClick={()=>setScreen('post')}><span>⚡</span><div><b>I NEED SOMETHING TO POST NOW</b><small>Get a complete ready-to-create post</small></div><i>›</i></button><button className="action secondary" onClick={()=>setScreen('week')}><span>✦</span><div><b>CREATE MY WEEK</b><small>Plan 7 days in one tap</small></div><i>›</i></button><h3 className="sectionTitle">YOUR WORKSPACE</h3><div className="grid"><button onClick={()=>setScreen('week')}><span>▦</span><b>Content Calendar</b><small>7 posts planned</small></button><button onClick={()=>setScreen('ideas')}><span>💡</span><b>Idea Bank</b><small>{saved.length} saved ideas</small></button><button onClick={()=>setScreen('pillars')}><span>◎</span><b>Content Pillars</b><small>{profile.pillars.length} active pillars</small></button><button onClick={()=>setScreen('setup')}><span>◒</span><b>My Strategy</b><small>Edit your setup</small></button></div><section className="tip"><span>COMPASS TIP</span><p>Consistency beats intensity. One useful post today is better than seven rushed posts next month.</p></section></>}{screen==='setup'&&<Page title="My Strategy" back={()=>setScreen('home')}><Setup profile={profile} save={persist}/></Page>}{screen==='post'&&<Page title="Today's Content" back={()=>setScreen('home')}><div className="chips"><span>🎯 {post.goal}</span><span>📱 {profile.platform} · {post.format}</span><span>◎ {post.pillar}</span></div><Card label="HOOK" text={post.hook}/><Card label="SCRIPT" text={post.script}/><Card label="SHOT LIST" text={post.shot}/><Card label="CAPTION" text={post.caption}/><Card label="CTA" text={post.cta}/><div className="buttonRow"><button onClick={copy}>📋 Copy Caption</button><button onClick={save}>💾 Save Idea</button></div><button className="wide" onClick={()=>{setIdx(idx+1);setToast('Fresh idea created ✦');setTimeout(()=>setToast(''),1200)}}>✦ Give me another idea</button></Page>}{screen==='week'&&<Page title="My Content Week" back={()=>setScreen('home')}><p className="intro">A balanced week for {profile.business||'your business'}.</p><div className="week">{[0,1,2,3,4,0,1].map((n,i)=>{const x=ideas[(idx+n)%ideas.length];return <article key={i} onClick={()=>{setIdx(idx+n);setScreen('post')}}><div className="day"><b>{['MON','TUE','WED','THU','FRI','SAT','SUN'][i]}</b><span>0{i+1}</span></div><div><small>{x.pillar} · {x.format}</small><h3>{x.hook}</h3></div><i>›</i></article>})}</div><button className="wide" onClick={()=>setScreen('post')}>⚡ Create today's post</button></Page>}{screen==='ideas'&&<Page title="Idea Bank" back={()=>setScreen('home')}>{saved.length?saved.map((x,i)=><section className="card" key={i} onClick={()=>setScreen('post')}><small>{x.pillar} · {x.format}</small><p>{x.hook}</p></section>):<div className="empty">💡<h2>Your best ideas live here.</h2><p>Save generated posts so you never lose a good idea.</p><button className="wide" onClick={()=>setScreen('post')}>Generate an idea</button></div>}</Page>}{screen==='pillars'&&<Page title="Content Pillars" back={()=>setScreen('home')}><p className="intro">Tap to choose what Content Compass should create around.</p><div className="pillars">{defaults.map(x=><button className={profile.pillars.includes(x)?'selected':''} onClick={()=>setProfile({...profile,pillars:profile.pillars.includes(x)?profile.pillars.filter(y=>y!==x):[...profile.pillars,x]})} key={x}>{profile.pillars.includes(x)?'✓ ': '+ '}{x}</button>)}</div><button className="wide" onClick={()=>persist(profile)}>Save pillars</button></Page>}</>}{toast&&<div className="toast">{toast}</div>}<nav><button className={screen==='home'?'active':''} onClick={()=>setScreen('home')}>⌂<small>Home</small></button><button onClick={()=>setScreen('week')}>▦<small>Plan</small></button><button className="plus" onClick={()=>setScreen('post')}>＋</button><button onClick={()=>setScreen('ideas')}>💡<small>Ideas</small></button><button onClick={()=>setScreen('pillars')}>◎<small>Strategy</small></button></nav></main>}
function Setup({profile,save}){const [p,setP]=useState(profile);return <section className="setup"><span className="eyebrow">PERSONALIZE YOUR COMPASS</span><h2>Tell us what you create.</h2><p className="intro">This stays on your device and shapes your content ideas.</p><label>WHAT DO YOU DO?<input value={p.business} onChange={e=>setP({...p,business:e.target.value})} placeholder="e.g. Social media trainer"/></label><label>WHO DO YOU WANT TO REACH?<input value={p.audience} onChange={e=>setP({...p,audience:e.target.value})} placeholder="e.g. Small business owners"/></label><label>MAIN GOAL<select value={p.goal} onChange={e=>setP({...p,goal:e.target.value})}><option>Grow my audience</option><option>Get more engagement</option><option>Generate leads</option><option>Make sales</option><option>Build authority</option></select></label><label>MAIN PLATFORM<select value={p.platform} onChange={e=>setP({...p,platform:e.target.value})}><option>Instagram</option><option>Facebook</option><option>TikTok</option><option>LinkedIn</option><option>YouTube</option></select></label><button className="wide" onClick={()=>save(p)}>✦ Build my content strategy</button></section>}
function Page({title,back,children}){return <section className="page"><div className="pageHead"><button onClick={back}>‹</button><div><small>CONTENT COMPASS</small><h1>{title}</h1></div></div>{children}</section>}
function Card({label,text}){return <section className="card"><small>{label}</small><p>{text}</p></section>}
